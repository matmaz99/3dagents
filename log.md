adding a place in the space for a mailbox area. This mailbow area is connected to a webhook. When a new post is done on the webhook (which is actually a payload of an email), a letter appear in that area. agent can interact with it by going to it and get then access to the JSON.

